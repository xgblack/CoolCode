module cool.web.adapter {
    // adapter层
    // web存放controller浏览器、scheduler定时器、consumer消息   其他外层适配器Adaptor
    // VO（View Object）
    // 负责对前端展示（web，wireless，wap）的路由和适配，对于传统B/S系统而言，adapter就相当于MVC中的controller
}